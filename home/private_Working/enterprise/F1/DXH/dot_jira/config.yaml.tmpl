{{- if has "F1" .decryptEnterprise }}
{{-   $top := merge . (dict
        "enterprise" (dict
          "name" "F1"
          "scope" "DXH"
        )
      )
}}
{{- includeTemplate "jira/config.yaml.tmpl" $top }}
{{- end -}}
