[env]
{{ $top := merge . (dict "enterprise" "F1") }}
{{- template "mise/env_jira.toml.tmpl" $top }}
{{- if ne .kind "container" }}
{{- template "mise/env_git.toml.tmpl" $top }}
{{- end }}
https_proxy = "socks5h://localhost:1080"
http_proxy = "socks5h://localhost:1080"
