{{- if ne .chezmoi.os "android" }}
# Can't use docker and container on Android
name: dive
method: binary
github:
  repo: wagoodman/dive
  release_asset:  dive_%1v_{{ .chezmoi.os }}_{{ .chezmoi.arch }}.tar.gz
archive:
  - dive
{{- end }}
