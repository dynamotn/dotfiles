{{- if .hasSound }}
name: cava
method: os
gentoo:
  packages:
    - name: media-sound/cava
arch:
  packages:
    - name: cava
ubuntu:
  packages:
    - name: cava
alpine: # Not need
termux: # Not need
macos:
  packages:
    - name: cava
{{- end }}
