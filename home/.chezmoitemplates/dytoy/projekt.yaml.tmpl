name: projekt
method: binary
is_essential: true
github:
  repo: dynamotn/projekt
  {{- $os := .chezmoi.os }}
  {{- if eq .chezmoi.os "android" }}
  {{-   $os = "linux" }}
  {{- end }}
  release_asset: projekt_{{ $os }}_{{ .chezmoi.arch }}.tar.gz
archive:
  - path: projekt
  - path: t
  - path: b
